# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
/sluice
/bin/
/dist/
/out/

# Test binary, built with `go test -c`
*.test

# Output of `go tool cover`
*.out
coverage.*

# Go workspace file
go.work
go.work.sum

# Dependency directory (rarely vendored, ignore if present)
/vendor/

# Env / secrets
.env
.env.*
!.env.example

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store

# Internal tooling — kept local in the vault as SoT, not for the public repo
/CLAUDE.md
/.planning/
/scripts/
